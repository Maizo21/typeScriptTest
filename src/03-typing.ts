let myProductName = "Phone";
let myProductPrice = 1000;

myProductName.toLowerCase();
myProductPrice.toFixed(2);

// myProductPrice.toLowerCase(); // Error: Property 'toLowerCase' does not exist on type 'number'.
// myProductName.toFixed(2); // Error: Property 'toFixed' does not exist on type 'string'.
